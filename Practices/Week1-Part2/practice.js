const readline = require("readline");

const rl = readline.createInterface(process.stdin, process.stdout);

rl.question("Why I am gay? (a) - Cuz you are bhenchod and chutiya. (b) - No", function gay(){

    if (gay.toUppercase() == 'A')
    {
        rl.question("What's on the other side? ", function(otherside){
            console.log(otherside + " is on the other side");
        rl.close();

    });
    } 
    else if (gay.toUppercase() == 'B')
    {
        rl.question("SO WHAT?", fucntion(gayIdeas){
            console.log("Gay");
            rl.close();
        })
    }
    } else {
        console.log("Invalid choice");
        rl.close();
    }
)